{
  "name": "@tvs/tvsm",
  "version": "0.1.0",
  "private": true,
  "description": "TVS Manager — unified CLI for end-users, mod developers, and content creators",
  "scripts": {
    "build": "npm run build:dist",
    "rebuild": "npm run build:dist",
    "build:dist": "npm run build:dist:module",
    "build:dist:module": "pwsh -NoProfile -ExecutionPolicy Bypass -File ./build/package.ps1",
    "tvsm": "pwsh -NoProfile -ExecutionPolicy Bypass -File ./tvsm.ps1"
  }
}
